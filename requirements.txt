# Tacode (Trajectory analysis code) -- pip requirements
#
#   python3 -m pip install -r requirements.txt
#
# or let setup_env.sh build a virtual environment for you:
#
#   ./setup_env.sh
#
# See the Requirements section of README.md for the full notes, including the
# standard libraries used and the versions verified by the developer.

numpy>=1.15.4
scipy>=1.4.0
PyYAML>=3.11
simplekml>=1.3.2

# Optional -- only needed if the GPX output in output_gpsdata.py is re-enabled.
# It is disabled in output_routine(), so Tacode runs without it.
# Install with: ./setup_env.sh --with-optional
#
# gpxpy>=1.3.5
